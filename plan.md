# TPC-H 高速化ロードマップ

SF=1 の実測プロファイルを基準に、費用対効果が高い順に進める。完了条件を満たした項目は `[x]` に更新する。

## 優先順位

- [x] 1. 集約を単一パス化する（目安: 2〜4日、Q1で1.2〜1.5倍）
  - [x] 現行のグループ化・集約経路と再走査箇所を特定する
  - [x] SELECT/HAVING 内の集約式を事前収集する
  - [x] グループごとの `COUNT` / `SUM` / `AVG` / `MIN` / `MAX` / `DISTINCT` 状態を入力1回で更新する
  - [x] グループの全行保持と集約式ごとの再走査を廃止する
  - [x] 空入力、NULL、DISTINCT、複数集約、HAVING、GROUP BY の回帰テストを追加する
  - [x] TPC-H Q1を実行し、結果一致と実行時間を記録する

- [x] 2. スキャン列絞り込みと早期フィルタを実装する（目安: 4〜8日、全体で1.5〜3倍）
  - [x] 全列の復号・コピー後にフィルタしている現行経路を特定する
  - [x] SELECT / WHERE / JOIN / GROUP BY / HAVING / ORDER BY から必要列を抽出する
  - [x] 行の射影付きデシリアライズと射影付きフルスキャンAPIを追加する
  - [x] 単一テーブル述語をスキャン中に評価し、不一致行の保持を避ける
  - [x] `SELECT *`、列なし `COUNT(*)`、別名、自己結合、相関サブクエリの安全なフォールバックを実装する
  - [x] EXPLAIN ANALYZEにスキャン行数・出力行数・復号列数を表示する
  - [x] 射影・早期フィルタの単体テストを追加する
  - [x] TPC-H Q6を実行し、結果一致と実行時間を記録する

- [x] 3. バッチ／ベクトル化実行へ移行する（目安: 2〜4週、2〜5倍）
  - [x] `DataChunk` / `ColumnVector` の実行インターフェースを設計する
  - [x] Scan → Filter → Project → Aggregate をバッチ化する
  - [x] 可変長値とNULLビットマップをバッチ表現へ統合する

- [x] 4. エグゼキューターを並列化する（目安: 1〜3週、4〜8コアで2.5〜6倍）
  - [x] morsel-driven parallel scanを実装する
  - [x] thread-local aggregateとマージを実装する
  - [x] partitioned hash joinと並列sortを実装する
  - [x] クエリ単位のCPU・メモリ制御を行うスケジューラを実装する

- [x] 5. PAXとデータスキップを導入する（目安: 3〜6週、scanで1.5〜3倍）
  - [x] ページ内列領域、visibility offset、NULL bitmapを設計する
  - [x] zone mapを追加する
  - [x] DATEの整数化、辞書圧縮、bit packingを評価・実装する

- [x] 6. 式評価を型付きbytecodeへ移行する（目安: 1〜2週、filter-heavyで1.3〜2倍）
  - [x] 式木から型付き命令列へのコンパイラを実装する
  - [x] バッチ式評価器と定数畳み込みを統合する

- [x] 7. LLVM JITを評価・導入する（目安: 4〜8週、全体で1.2〜2倍）
  - [x] bytecode実装後のプロファイルでJIT対象を限定する
  - [x] コンパイル時間を含む短時間クエリの損益分岐を測定する
  - [x] filter / projection / aggregate kernelから段階的に導入する

## 計測記録

- 基準値（SF=1、全22問合計）: 約495.5秒
- 基準値 Q1: 約26.32秒
- 基準値 Q6: 約19.79秒
- 実装後 Q1: 約20.64秒（約1.28倍、基準比21.6%短縮）
- 実装後 Q6: 約13.08秒（約1.51倍、基準比33.9%短縮）
- 実装後 全22問合計: 約410.47秒（約1.21倍、基準比17.2%短縮）
- LLVM JIT microbenchmark: compile約2.36ms、INT64 filter kernel約2.5倍、損益分岐約2,097万評価
- 回帰確認: Q18のみ約32.82秒から約34.38秒へ4.8%低下。ほか21問は完走し、全体では約85.0秒短縮
- テスト: 390/390成功、既存の無効化テスト1件は未実行（CTest全件、2026-08-19）
